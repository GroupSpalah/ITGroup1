1) Write a program that print numbers from 1 to 10 (use any loop) in
separated thread.

2) Write a program that run 15 threads. Every thread must print to the
console random value from 10 to 100

3) Имеется 5 потоков с именами. Необходимо им поручить задание считать один
      и тот же файл(Story.txt) и подсчитать сколько раз  в нем встречается слово(holidays) и вывести время,
      которое потратил каждый поток.

   Использовать join  и без него.

