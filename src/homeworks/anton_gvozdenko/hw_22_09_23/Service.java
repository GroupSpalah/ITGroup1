package homeworks.anton_gvozdenko.hw_22_09_23;




import java.io.IOException;

import java.util.Comparator;
import java.util.List;
import java.util.Optional;


public class Service {

  /*  public void showObjectMaxSquare() throws IOException, ClassNotFoundException {
     List<Rectangle> rectangles = Deserializator.read();
      Comparator<Rectangle> rectangleComparator = Comparator.comparing(Rectangle::calculateArea);

        Optional<Rectangle> maxSquare = rectangles
                .stream()
                .max(rectangleComparator);
        if (maxSquare.isPresent()) {
            Rectangle rectangle = maxSquare.get();
            System.out.println(rectangle);
*/
        }







